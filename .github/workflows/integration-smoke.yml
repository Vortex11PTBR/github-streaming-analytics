name: CI Integration Smoke

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  integration-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Start docker compose services
        run: |
          docker compose up -d
      - name: Wait for Kafka and Schema Registry to be ready
        run: |
          for i in `seq 1 60`; do
            docker compose ps | grep -q kafka && docker compose ps | grep -q schema-registry && break || sleep 2;
          done
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11
      - name: Install runtime deps
        run: |
          python -m pip install --upgrade pip
          pip install kafka-python requests fastavro
      - name: Register schema and produce Avro messages
        run: |
          python scripts/register_schema_and_produce.py --count 3
      - name: Run smoke test (consume produced messages)
        run: |
          python scripts/smoke_test.py
      - name: Tear down compose
        if: always()
        run: docker compose down
